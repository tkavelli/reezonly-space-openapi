# 📋 Гайд по прогону линтера OpenAPI

## Процесс работы с ошибками линтера

### 1. Прогон линтера по модульным файлам

Прогоняй линтер **по каждому файлу отдельно**, начиная с самых простых:

```bash
# Основные модули
npx redocly lint specs/reports.yaml --format summary
npx redocly lint specs/certificates.yaml --format summary
npx redocly lint specs/learning.yaml --format summary
npx redocly lint specs/groups.yaml --format summary
npx redocly lint specs/users.yaml --format summary

# Общие компоненты (core)
npx redocly lint specs/core/base.yaml --format summary
npx redocly lint specs/core/common/parameters.yaml --format summary
npx redocly lint specs/core/common/responses.yaml --format summary
npx redocly lint specs/core/common/schemas.yaml --format summary
```

### 2. Интерпретация результатов

Линтер возвращает формат:
```
error   rule-name: COUNT
warning rule-name: COUNT
```

**Самые часто встречаемые ошибки:**

| Ошибка | Причина | Как исправить |
|--------|---------|---------------|
| `operation-description` | Операция не имеет поля `description` | Добавь текстовое описание что делает метод |
| `security-defined` | Операция использует undefined security scheme | Определи security в `components.securitySchemes` или удали ненужные |
| `scalar-property-missing-example` | Скалярное свойство (string, integer) без примера | Добавь `example: "значение"` в свойство |
| `no-empty-servers` | Пусто поле `servers` | Либо удали `servers`, либо заполни реальные URL |
| `parameter-description` | Параметр без описания | Добавь `description: "..."`  |
| `no-unused-components` | Определение которое не используется | Удали неиспользуемые схемы/параметры |
| `struct` | Ошибка структуры YAML | Проверь синтаксис, отступы, кавычки |

### 3. Стратегия исправления по приоритетам

1. **HIGH** — блокирующие ошибки:
   - `struct` — синтаксис YAML
   - `no-unresolved-refs` — ломанные ссылки
   - `no-empty-servers` — пустые servers

2. **MEDIUM** — логические ошибки:
   - `operation-description` — отсутствующие описания
   - `parameter-description` — отсутствующие описания параметров
   - `security-defined` — неопределённая безопасность

3. **LOW** — качество документации:
   - `scalar-property-missing-example` — примеры значений
   - `operation-4xx-response` — ошибочные коды ответов

### 4. Команды для быстрого анализа

**Найти операции без description:**
```bash
python3 << 'EOF'
import yaml

with open('specs/users.yaml', 'r', encoding='utf-8') as f:
    data = yaml.safe_load(f)

ops_without = []
for path, methods in data.get('paths', {}).items():
    for method, op in methods.items():
        if isinstance(op, dict) and 'operationId' in op:
            if 'description' not in op:
                ops_without.append(f"  {method.upper():6} {path:40} - {op.get('operationId')}")

print(f"Операций без description: {len(ops_without)}\n")
for op in ops_without:
    print(op)
EOF
```

**Получить полный отчет JSON:**
```bash
npx redocly lint specs/users.yaml --format json > lint-report.json
```

### 5. Workflow исправления

1. Прогни линтер: `npx redocly lint specs/ФАЙЛ.yaml --format summary`
2. Определи тип ошибки и количество
3. Отредактируй файл (Edit tool)
4. Повтори шаг 1 и проверь уменьшилось ли количество ошибок
5. Переходи к следующему файлу

### 6. Проверка всей спеки

Когда все модули готовы:
```bash
npm run lint  # главная спека
npm run bundle  # объединение всех модулей
```

## История исправлений

### Исправленные ошибки:
- ✅ 9 операций: добавлены описания (`operation-description`)

### Оставшиеся ошибки:
- ❌ 56 ошибок: отсутствуют примеры в скалярных свойствах (`scalar-property-missing-example`)
- ❌ 16 ошибок: security schemes не определены (`security-defined`)
- ❌ 5 ошибок: описания параметров (`parameter-description`)
- ❌ 2 ошибки: неразрешённые ссылки (`no-unresolved-refs`)
- ❌ 1 ошибка: структура YAML (`struct`)
- ❌ 1 ошибка: пустые servers (`no-empty-servers`)

## Советы

- Не пытайся исправить всё сразу, работай по модулям
- Сначала исправь `struct` и `no-unresolved-refs` — они ломают весь парсинг
- Потом добавь все `description` для операций и параметров
- В конце добавляй `example` для лучшей документации
- Проверяй что изменения не сломали другие файлы: `npm run bundle`
