apis:
  space:
    root: ./space-platform-api.yaml

extends:
  - recommended

theme:
  openapi:
    logo:
      src: img/logo.svg
      altText: Reezonly LMS
      gutter: 16px
      maxHeight: 40px
    colors:
      primary:
        main: '#514df7'
      text:
        primary: '#1f2430'
        secondary: '#4b5565'
      sidebar:
        backgroundColor: '#eff1f8'
      rightPanel:
        backgroundColor: '#0f172a'
        textColor: '#e2e8f0'
    typography:
      fontFamily: 'StyreneALC, "Inter", "Helvetica Neue", Arial, sans-serif'
      headings:
        fontFamily: 'StyreneALC, "Inter", "Helvetica Neue", Arial, sans-serif'
        fontWeight: 700
      code:
        fontFamily: '"SFMono-Regular", "Menlo", "Monaco", "Consolas", "Liberation Mono", "Courier New", monospace'
    sidebar:
      width: 280px
    rightPanel:
      width: 40%

rules:
  no-unused-components: error
  no-unresolved-refs: error
  operation-description: error
  parameter-description: error
  operation-operationId-unique: error
  operation-tag-defined: error
  scalar-property-missing-example: error
  # кастом: operationId должен начинаться с глагола
  rule/operation-id-verb:
    subject:
      type: Operation
      property: operationId
    assertions:
      pattern: "^(get|list|create|update|delete|download|assign)[A-Z].+"
    severity: error
    message: "operationId must start with get|list|create|update|delete|download|assign."
