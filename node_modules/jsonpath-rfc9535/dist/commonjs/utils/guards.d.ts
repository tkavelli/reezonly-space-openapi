import type { JsonValue } from "../core/results.ts";
import type { StackItem } from "../core/types.ts";
export declare function isPlainObject(value: unknown): value is Record<string, unknown>;
export declare const isStackItemWithArrayValue: (item: StackItem) => item is StackItem<JsonValue[]>;
export declare const isStackItemWithObjectValue: (item: StackItem) => item is StackItem<Record<string, JsonValue>>;
