import type { JsonValue } from "./results.ts";
import type { Callback } from "./types.ts";
export type Options = {
    capturePaths: boolean;
};
export default function exec(input: JsonValue, expression: string, opts: Options, cb: Callback): void;
