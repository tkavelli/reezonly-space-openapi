# Reezonly Space Platform API Documentation

## 📖 Обзор

Это документация для **Reezonly Space Platform API** - RESTful API для интеграции с образовательной платформой Space.

## 🚀 Быстрый старт

### Просмотр документации

1. **Откройте документацию в браузере:**
   ```
   http://localhost:3000
   ```

2. **Или скачайте готовый HTML файл:**
   - Откройте `index.html` в браузере
   - Используется Redoc 2.1.3 + кастомные кнопки копирования метода/пути

### Линт спеки (рекомендуется перед коммитом)

Из каталога `openapi/tools`:
```
npm run lint
```
Скрипт валидирует `bundles/complete-api.yaml` через `@apidevtools/swagger-parser`.

### Сборка из модулей (45 публичных методов)

```
cd openapi/tools
npm install   # один раз
npm run build # собирает из specs/* в bundles/complete-api.yaml и sync в space-platform-api.yaml
```

После сборки dev-сервер можно запустить:
```
cd openapi
python3 -m http.server 3000
```
Актуальный бандл с cache-bust: `/bundles/complete-api.yaml?v=2025-11-20-01`.

### 🚢 Одной кнопкой через Docker

Полный пайплайн (сборка 45 методов + HTML) упакован в образ:

```
cd openapi
docker build -t reezonly-space-openapi .
docker run -p 3000:80 reezonly-space-openapi
```

Что происходит внутри:
- Stage `build`: `npm ci` (root и `tools/`), `npm run build` (сшивает 45 публичных методов), `npm run bundle`, `npm run build-docs`.
- Stage `runtime`: Nginx раздаёт `docs/index.html` на `http://localhost:3000`, сырые спеки по путям `/bundles/complete-api.yaml` и `/space-platform-api.yaml`.

Проверка локально: открыть `http://localhost:3000` в браузере или скачать YAML по прямым ссылкам.

### Базовая информация

- **Базовый URL:** `https://your-space-domain.com/api/v1`
- **Аутентификация:** `X-API-Key` header
- **Формат данных:** JSON
- **Кодировка:** UTF-8

## 🔐 Аутентификация

Все запросы к API должны содержать заголовок:

```http
X-API-Key: your_api_key_here
```

## 🏗️ Структура API

API организован по доменам с вложенной структурой:

### 👥 Управление пользователями (Users)
- **Users.Operations** - CRUD операции с пользователями
- **Users.Courses** - Управление доступом к курсам
- **Users.Import** - Импорт пользователей
- **Users.Fields** - Кастомные поля

### 🏢 Управление группами (Groups)
- **Groups.Structure** - Иерархия и структура групп
- **Groups.Members** - Управление участниками

### 🏆 Сертификаты (Certificates)
- **Certificates.Templates** - Шаблоны сертификатов (read-only)
- **Certificates.Issued** - Выданные сертификаты
- **Certificates.Lookups** - Справочники

### 📚 Курсы (Courses)
- Информация о курсах (read-only)
- Каталог обучающих продуктов
- **Pages** - страницы/лендинги (read-only)
- **Integration Reports** - JSON-версия сводного отчёта

## 📋 Покрытие

- Всего публичных методов: **45** (строго по `API_INTEGRATION_METHODS_FINAL_priority.md`).
- Категории: Users, Groups, Certificates, Learning (Courses/Pages), Integration Reports.

## 🔧 Форматы ответов

### Успешный ответ

```json
{
  "success": true,
  "data": { ... },
  "meta": {
    "total": 100,
    "page": 1,
    "per-page": 20
  }
}
```

### Ошибка

```json
{
  "success": false,
  "errors": {
    "field": ["Error message"]
  }
}
```

## 🌍 Мультиязычность

API поддерживает 2 языка:
- `ru` - Русский (по умолчанию)
- `en` - English

Укажите язык в заголовке:

```http
Accept-Language: ru
```

## ⚠️ Ограничения

- **Rate limiting:** Ограничение запросов в минуту
- **Batch size:** Максимум 1000 записей за раз
- **Async operations:** Тяжелые операции выполняются в фоне

## 📚 Примеры использования

### Получить список пользователей

```bash
curl -X GET "https://your-space-domain.com/api/v1/user/user/index?page=1&per-page=20" \
  -H "X-API-Key: your_api_key" \
  -H "Accept-Language: ru"
```

### Создать пользователя

```bash
curl -X POST "https://your-space-domain.com/api/v1/user/user/create" \
  -H "X-API-Key: your_api_key" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "name": "Иван Иванов",
    "password": "SecurePass123!",
    "role": "student"
  }'
```

## 🛠️ Локальная разработка

Для запуска локального сервера с документацией:

```bash
# Из корня openapi
python3 -m http.server 3000 # стартует dev-сервер
open http://localhost:3000  # открыть в браузере
```

## 📞 Поддержка

- **Email:** api-support@reezonly.com
- **Документация:** https://reezonly.com/docs
- **Статус:** https://status.reezonly.com

## 📝 Лицензирование

Коммерческая лицензия. См. https://reezonly.com/license

---

*Документация сгенерирована с использованием OpenAPI 3.1.0 и Redoc*
